node_modules/

# secrets / auth state — never commit
token.json
credentials.json
storage-state.json

# real flight config is personal (routes/dates) — keep only the example
config/flights.json

# local data
data/*.db
data/*.db-journal
data/*.db-shm
data/*.db-wal
*.log

# personal deployment details (hostnames/IPs, host paths)
DEPLOY-NOTES.md

# OS
.DS_Store
